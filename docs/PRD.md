# PRD — 건설업 시공능력평가 통합 조회 서비스

| 항목 | 내용 |
|---|---|
| 문서 버전 | 1.1 (RAW_new 원본 파일 명세 반영) |
| 작성일 | 2026-07-05 (v1.1: 2026-07-06) |
| 데이터 기준 | 2025년 시공능력평가 공시 |
| 산출물 | `construction_capability_search_v5.html` (단일 파일 조회기) |
| 업데이트 주기 | **매년 8월 2일** (시공능력평가 연간 공시 직후) |

---

## 1. 개요

### 1.1 배경
건설업 시공능력평가액은 공종(업종)별로 서로 다른 협회가 공시한다. 발주·입찰 실무에서 특정 업체의 공종별 평가액과 순위를 확인하려면 협회별 사이트를 각각 조회해야 하며, 협회마다 파일 포맷·컬럼 구성·금액 단위가 달라 직접 비교가 불가능하다.

### 1.2 목표
1. 6개 대분류(종합건설·전문건설·기계설비·소방설비·전기설비·정보통신)의 시공능력평가 데이터를 **하나의 표준 스키마로 정규화**한다.
2. 정규화된 전체 데이터(약 14.3만 건)를 내장한 **단일 HTML 조회기**를 제공한다. 서버·설치 없이 브라우저만으로 동작한다.
3. 매년 8월 2일 기준으로 재수집·재생성이 가능하도록 **수집 방법·변환 규칙·실행 스크립트를 코드와 문서로 보존**한다.

### 1.3 비목표
- 실시간 조회(협회 API 연동)는 하지 않는다. 연 1회 스냅샷 방식이다.
- 시공능력평가액 외 재무제표, 수주실적 등 추가 데이터 통합은 범위 밖이다.

---

## 2. 데이터 흐름 (파이프라인)

```
[1] 수집 (매년 8월 2일)
    ├─ 협회 파일 다운로드 (5개 대분류, xlsx·csv 혼합) → RAW_new/ 에 원본 그대로 보관
    └─ KECA 크롤링 (전기설비)                 → scripts/crawl_keca.py
            │
[2] 표준 raw CSV 변환                          → scripts/convert_raw.py
    (원본별 시트·헤더·인코딩 처리)             → raw/*.csv (14컬럼 표준 헤더)
            │
[3] 정규화                                     → scripts/normalize.py
    ├─ 금액 단위 통일 (백만원)
    ├─ 숫자/문자열 정제, 결측 처리
    ├─ 순위 검증·재산정 (전문건설·기계설비)
    └─ 품질 검증 리포트
            │                                  → data/normalized.json (git 미추적)
[4] 산출물 생성                                → scripts/build_html.py
            │
[5] 배포                                       → construction_capability_search_v5.html 커밋·푸시
```

---

## 3. 데이터 소스 명세

6개 대분류는 각각 별도 기관이 공시한다. **전기설비만 크롤링**이고 나머지 5개는 협회 사이트에서 파일(xlsx·csv 혼합)을 다운로드해 `RAW_new/`에 원본 그대로 보관한다. 원본 → 표준 raw CSV 변환은 `scripts/convert_raw.py`가 수행한다.

| 대분류 | 제공 기관 | 획득 방식 | 원본 파일 (RAW_new/, 2025) | 행수(2025) | 원본 금액 단위 |
|---|---|---|---|---|---|
| 종합건설 | 대한건설협회(CAK) | xlsx 다운로드 | `CAK_25년도 종합건설사업자 시공능력평가결과(누리집).xlsx` | 19,610 | 백만원 |
| 전문건설 | 대한전문건설협회 | xlsx 다운로드 | `전문건설업시공능력평가공시(2025)_업종.xlsx` | 76,118 | 백만원 |
| 기계설비 | 대한기계설비건설협회 | xlsx 다운로드 | `2025년 기계설비·가스공사업 시공능력평가 공시 현황.xlsx` | 7,694 | 백만원 |
| 소방설비 | 한국소방시설협회 | xlsx 다운로드 | `CCE_2025.xlsx` | 6,845 | **천원** (원본 명시) |
| 전기설비 | 한국전기공사협회(KECA) | **크롤링** (§4) | (크롤링 산출물) | 21,179 | **천원** |
| 정보통신 | 한국정보통신공사협회(KICA) | csv 다운로드 | `KICA_rank_202607061000.csv` | 11,836 | **천원** (헤더 명시) |

> ⚠ 단위 주의: `dataset-map.json`에 소방·전기·정보통신 단위가 "원"으로 표기되어 있었으나, CCE 원본의 "단위: 천원" 표기와 KICA 헤더 `시공능력평가액(천원)`으로 **천원 단위**가 원본에서 확정되었다.

### 3.1 소스별 원본 파일 상세

#### 종합건설 — 대한건설협회 `CAK_*.xlsx`
- **시트 5개** = 공종: 토건(→토목건축공사업), 토목(→토목공사업), 건축(→건축공사업), 산설(→산업환경설비공사업), 조경(→조경공사업)
- 헤더 3~5행 병합, **데이터 6행부터**
- 컬럼: 순위, 상호, 대표자, 소재지, 전화번호, 등록번호, 시공능력평가액, 항목별 평가액(공사실적·경영·기술능력·신인도), [토건 시트만: 토목분야액·건축분야액·실적(토목)·실적(건축)], (직전년도)건설공사실적, 보유기술자수
- `순위`는 실제 평가액 순위 (삼성물산=1) → 그대로 사용
- 등록번호에 선행 공백(`   668`) — strip 처리
- 다운로드 위치: 대한건설협회 시공능력평가 공시 페이지 *(정확한 메뉴 경로/URL은 §11 O3 — 다음 수집 시 기록)*

#### 전문건설 — 대한전문건설협회 `전문건설업시공능력평가공시(YYYY)_업종.xlsx`
- **시트 12개** = 공종 (지반조성·포장, 실내건축, 금속·창호·지붕·건축물조립, 도장·습식·방수·석공, 조경식재·시설물, 철근·콘크리트, 구조물해체·비계, 상·하수도, 철도·궤도, 철강구조물, 수중·준설, 승강기·삭도) — 시트명이 그대로 중분류
- 헤더 1~3행(2단), **데이터 4행부터**
- 컬럼: 연번, 상호, 대표자, 소재지, 전화번호, 등록번호, 시공능력평가액 총액, 공사실적·경영·기술능력·신인도 평가액, (직전년도)건설공사실적, 기술자수, 비고
- ⚠ **`연번`은 가나다순 일련번호이며 순위가 아니다.** (2025: 지반조성·포장 평가액 1위 삼호개발(주) 421,668백만원의 연번이 4818) → **순위 재산정** (§6.4). v4까지는 이 연번이 순위로 잘못 노출되었다.
- 동일 업체가 여러 공종에 중복 등록 가능 — 중복 제거하지 않는다
- 같이 배포되는 `..._주력분야.xlsx`(19시트, 주력분야별 공시)는 **현재 미사용** (§11 O5)

#### 기계설비 — 대한기계설비건설협회 `YYYY년 기계설비·가스공사업 시공능력평가 공시 현황.xlsx`
- 시트 1개(기계설비·가스공사업), 헤더 3행, **데이터 4행부터**
- 컬럼: 연번, 상호, 대표자명, 소재지, 전화번호, 건설업 등록번호, 시공능력평가액, 공사실적·경영·기술능력·신인도 평가액, 건설공사실적, 보유기술인수, 비고
- ⚠ 원본에 **"(상호 가나다순)" 명시** — `연번`은 순위가 아님 (2025: 평가액 1위 한양이엔지(주)의 연번 7221) → **순위 재산정** (§6.4)

#### 소방설비 — 한국소방시설협회 `CCE_YYYY.xlsx`
- 시트 1개(Ranking List), 헤더 3행(1행 제목, 2행에 기준일자·**"단위: 천원"**), **데이터 4행부터**
- 컬럼: 순번, 구분, 지역, 상호, 대표자, 업종, 등록번호, 시공능력평가액, 전국순위, 지역순위
- ⚠ **`전국순위`는 업종별로 따로 매겨진다** — 업종: 전문(6,660) / 일반(전기)(149) / 일반(기계)(36). 따라서 중분류를 `소방시설공사업(전문)` / `소방시설공사업(일반·전기)` / `소방시설공사업(일반·기계)` 3개로 분리해 순위 유효성을 유지한다. 표준 `순위`에는 전국순위 사용 (`순번`은 전체 일련번호)
- 숫자가 `1.0` float — 정수 변환. 등록번호 형식 혼재(`제강남2001-20호`, `2022-02-00383`) — 문자열 보존
- ⚠ 파일의 차원 메타데이터가 깨져 있어 openpyxl read_only 모드에서 1행으로 보임 → `reset_dimensions()` 필요 (convert_raw.py에 반영)

#### 전기설비 — 한국전기공사협회 (크롤링)
- 공종 1종: 전기공사업. 유일한 크롤링 소스 — 상세 방법은 §4
- 소재지가 도로명 주소 전체로 수집됨 (타 소스는 시/도 수준) — 그대로 보존
- 등록번호 형식: `경기-00093` (시도-일련번호)
- 크롤링 상세 페이지에서 지역순위·전국순위 모두 제공 → 표준 `순위`에는 **전국순위** 사용

#### 정보통신 — 한국정보통신공사협회 `KICA_rank_YYYYMMDDHHMM.csv`
- **CP949(EUC-KR) 인코딩 CSV** — UTF-8로 읽으면 깨짐, convert_raw.py가 처리
- 컬럼: 순위, 등록번호, 상호, 시공능력평가액(천원) — 단위가 헤더에 명시
- `순위`는 실제 평가액 순위 → 그대로 사용
- **대표자·소재지 미제공** — 빈 값 유지. 금액은 `" 289,006,000"` 콤마·공백 포함 — 정제 후 변환

> 참고: 종합·전문·기계 원본에는 **전화번호** 컬럼이 있으나 현재 표준 스키마에 포함하지 않는다 (필요 시 §5.1 확장). **사업자등록번호는 2025년 기준 6개 원본 어디에도 없다** (§6.3, §11 O2).

---

## 4. KECA(전기설비) 크롤링 명세

> 2025년 수집 시 검증된 방법. 매년 재크롤링 전 §4.6 체크리스트를 먼저 확인한다.
> 참조 구현: `scripts/crawl_keca.py`

### 4.1 대상
- 모바일 회원사 검색: `https://m.keca.or.kr/service/service07.do?menuCd=4051`
- 목록 페이지(업체명·대표자·주소 일부·내부 license ID) + 상세 페이지(등록번호·시공능력평가액·지역/전국순위) **조합**으로 수집해야 한다. 목록만으로는 평가액을 얻을 수 없다.

### 4.2 요청 구조
| 구분 | 방식 | URL | 비고 |
|---|---|---|---|
| 초기 진입 | GET | `/service/service07.do?menuCd=4051` | 세션·CSRFToken 확보 |
| 목록 조회 | **POST** | `/service/service07.do` | `gubun=page` |
| 상세 조회 | **POST** | `/service/service07D.do` | `gubun=detail` |

- 폼의 원래 `method="get"`이지만 자바스크립트(`js_searchAction`)가 POST로 강제 변경한다. **GET으로 구현하면 안 된다.**
- hidden 필드: `menuCd`, `currentPageNo`, `license`, `gubun`, `CSRFToken` — CSRFToken은 세션과 함께 매 요청에 포함.

### 4.3 파라미터
- 목록: `menuCd=4051`, `currentPageNo=<페이지>`, `license=`(빈값), `gubun=page`, `searchSido`, `searchSigungu`, `searchGubun`(company/daepyo/licenNo), `searchText`, `CSRFToken`
- 상세: 위와 동일하되 `license=<내부 license ID>`, `gubun=detail`
- 상세 진입 키는 화면의 등록번호가 아니라 목록의 `onclick="js_detailAction('12720521')"`에 들어있는 **내부 license ID**다.

### 4.4 추출 데이터
- 목록: 상호(`<div class="name">[경남-00408] 125전기공사</div>` 형식), 대표자, 주소, license ID
- 상세: 등록번호, 등록일, 관할시도회, 상호, 대표자, 소재지, **시공능력평가액**(th 라벨 `시공능력<br/>평가액` 행의 td), 지역순위, 전국순위
- 순위·평가액에 문구·공백이 섞이므로 정규식으로 숫자만 추출. 원본 문자열과 정제 숫자를 **둘 다 저장**한다.
- 저장 컬럼: `license_id, 등록번호, 등록일, 관할시도회, 상호, 대표자, 소재지, 시공능력평가액_원문, 시공능력평가액, 지역순위, 전국순위`

### 4.5 규모·운영
- 2025년 기준 총 21,179건, 페이지당 10건 → 목록 약 2,118회 + 상세 약 21,179회 요청
- **중간 저장 필수**: `SAVE_INTERVAL` 페이지마다 중간 파일 저장 (`keca_시공능력평가액_중간_0100p.xlsx` 식 명명), 실패 license ID는 별도 파일로 기록해 부분 재수집
- 재시작 시 `START_PAGE`부터 이어받기. 첫 실행은 `END_PAGE=3` 정도로 소량 테스트 후 전체 실행
- Colab 등 세션이 끊길 수 있는 환경에서는 중간 저장 주기를 짧게. 결과 메일 발송 자동화 시 Gmail은 앱 비밀번호 기반 SMTP 필요

### 4.6 재크롤링 체크리스트 (매년 8/2 실행 전)
- [ ] 목록/상세 URL이 여전히 `/service/service07.do`, `/service/service07D.do`인가
- [ ] hidden 필드명(`menuCd`,`currentPageNo`,`license`,`gubun`,`CSRFToken`)이 그대로인가
- [ ] `js_linkPage`, `js_detailAction`, `js_searchAction` 로직이 유지되는가
- [ ] 총 건수·마지막 페이지 번호 변경 확인
- [ ] 상세 페이지의 `시공능력평가액`, `지역순위`, `전국순위` 라벨 문자열 변경 여부
- [ ] 지역 필터 변경 시 `searchSido`/`searchSigungu` 로직 재점검

---

## 5. 표준 데이터 스키마

### 5.1 raw CSV 표준 헤더 (파이프라인 입력)
원본(§3.1, xlsx·csv 혼합)을 `scripts/convert_raw.py`가 아래 헤더의 UTF-8(BOM) CSV로 변환해 `raw/`에 둔다. 소스가 제공하지 않는 컬럼은 빈 값.

```
대분류,중분류,순위,상호,대표자,소재지,등록번호,시공능력평가액,공사실적평가액,경영평가액,기술능력평가액,신인도평가액,건설공사실적,기술자수
```

- **선택 컬럼 `사업자번호`**: 원본에 사업자등록번호(10자리)가 있는 소스는 표준 헤더 뒤에 `사업자번호` 컬럼을 추가할 수 있다. 파이프라인이 자동 인식한다 (`사업자번호` 또는 `사업자등록번호` 헤더 모두 허용).

### 5.2 정규화 레코드 (파이프라인 출력, HTML 내장 JSON)
| 필드 | 타입 | 원본 컬럼 | 설명 |
|---|---|---|---|
| `cat` | string | 대분류 | 종합건설/전문건설/기계설비/소방설비/전기설비/정보통신 |
| `sub` | string | 중분류 | 공종명 |
| `rank` | int | 순위 | 공종 내 시공능력평가액 순위 (§6.4) |
| `name` | string | 상호 | 업체명 |
| `rep` | string | 대표자 | 미제공 시 빈 문자열 |
| `addr` | string | 소재지 | 미제공 시 빈 문자열 |
| `reg` | string | 등록번호 | 건설업/공사업 등록번호, 문자열 보존 |
| `bizno` | string(선택) | 사업자번호 | **원본에 있을 때만 포함.** 하이픈 제거 10자리로 정규화 |
| `amt` | number | 시공능력평가액 | **백만원 통일** (§6.2) |

---

## 6. 정규화 규칙

구현: `scripts/normalize.py`

### 6.1 인코딩·파싱
- 입력 CSV는 `utf-8-sig`로 읽는다 (BOM 제거).
- 금액·숫자 필드: 콤마 제거 후 float 변환. 변환 실패 시 해당 행을 리포트에 기록하고 값은 결측 처리.
- `순위`: `1.0` 같은 float 표기는 int로 변환.
- `등록번호`: 문자열 보존. 단, `668.0`처럼 float 변환 흔적이 있으면 `.0` 접미사 제거.
- 문자열 필드는 `strip()` 후 저장, 결측은 빈 문자열.

### 6.2 금액 단위 통일 (→ 백만원)
| 소스 | 원본 단위 | 변환 |
|---|---|---|
| 종합건설, 전문건설, 기계설비 | 백만원 | 그대로 (÷1) |
| 소방설비, 전기설비, 정보통신 | 천원 | **÷1,000** |

### 6.3 사업자번호 (조건부)
- raw CSV에 `사업자번호`/`사업자등록번호` 컬럼이 있으면 하이픈·공백 제거 후 `bizno`로 포함. 10자리 숫자가 아니면 경고 리포트.
- 컬럼이 없는 소스는 `bizno` 필드 자체를 생성하지 않는다.
- **산출물 동작**: 내장 데이터에 `bizno`가 1건이라도 있으면 결과 테이블·CSV 내보내기에 '사업자번호' 컬럼이 자동 표시되고, 없으면 컬럼 자체가 나타나지 않는다.
- **2025년 확인 결과: 6개 원본 어디에도 사업자등록번호가 없다** → 결과 화면 표시 요청은 폐기(§11 O2). 조건부 지원 코드는 유지 — 향후 원본에 컬럼이 생기면 재빌드만으로 자동 표시된다.

### 6.4 순위 정책
- 기본: 소스 제공 순위 사용 (`rank_policy: source`) — 종합건설(CAK), 소방설비(전국순위), 전기설비(전국순위), 정보통신
- **전문건설·기계설비: 평가액 내림차순 재산정** (`rank_policy: recompute`) — 원본의 `연번`이 가나다순 일련번호이기 때문 (§3.1). v4까지 두 대분류(약 8.4만 건)의 순위가 잘못 노출되었다.
- 소방설비는 전국순위가 업종별로 매겨지므로 중분류를 업종별 3개로 분리해 순위 유효성 확보 (§3.1)
- 검증: 모든 소스에 대해 "공종 내 평가액 최댓값 행의 순위 = 1" 여부를 확인하고, 불일치하면 경고 리포트 (다른 소스의 순위 오염 조기 감지)

### 6.5 중복 정책
- (대분류, 중분류, 상호) 단위로만 중복을 점검한다. 전문건설의 다공종 등록은 정상이므로 대분류 간 중복은 허용.

---

## 7. 데이터 품질 검증

`normalize.py`가 실행 시 자동 수행하고, 하나라도 실패하면 종료 코드 ≠ 0 (산출물 생성 중단).

| # | 검증 | 기준 |
|---|---|---|
| Q1 | 파일 존재·비어있지 않음 | 6개 raw 파일 모두 헤더+1행 이상. **빈 파일(2바이트)은 즉시 실패** |
| Q2 | 최소 행수 | 전년 대비 –20% 이상 감소 시 실패 (2025 기준값: §3 표) |
| Q3 | 필수 컬럼 | 대분류·중분류·순위·상호·시공능력평가액 결측률 1% 미만 |
| Q4 | 금액 범위 | 변환 후 amt가 0 초과, 공종 1위가 상식적 규모인지 (종합 1위 > 10조, 전기 1위 > 1조 등) — 단위 변환 누락 감지 |
| Q5 | 순위 정합성 | 공종 내 최대 amt 행의 rank = 1 |
| Q6 | 사업자번호 형식 | bizno 존재 시 10자리 숫자 비율 99% 이상 |

---

## 8. 산출물(HTML 조회기) 요구사항

단일 HTML 파일. 데이터 JSON 내장, 외부 요청 없음(오프라인 동작). 생성: `scripts/build_html.py`

### 8.1 기능 (v4 유지)
- **업체명 검색**: 최대 10개 동시 입력, 부분일치, 입력 순서별 색상 구분
- **공종 필터**: 중분류 다중 선택 최대 3개
- **예정가격 판정**: 예정가격(백만원) 입력 시 각 행에 초과(✓)/미달(✗) 표시
- **정렬**: 모든 컬럼 헤더 클릭 정렬 (토글)
- **행 제외**: 결과에서 개별 행 삭제(✕)
- **CSV 내보내기**: 현재 결과를 UTF-8 BOM CSV로 다운로드
- 결과 컬럼: #, 대분류, 공종(중분류), 업체명, 대표자, 소재지, 평가액(백만원), 공종내순위, 제외

### 8.2 v5 변경사항
1. **사업자번호 조건부 컬럼**: 데이터에 `bizno`가 존재하면 '소재지' 다음에 '사업자번호' 컬럼 자동 표시 + CSV 내보내기에도 포함. 없으면 완전 숨김
2. **업데이트 주기 표기**: 헤더에 "기준: {연도}년 공시 · 매년 8월 2일 갱신" 배지
3. **버그 수정**: v4 `doSearch()`의 상태바 문구 생성부가 미정의 변수 `cat`을 참조해 검색 후 상태바가 갱신되지 않던 문제(ReferenceError) 수정
4. 기계설비 순위가 재산정된 값으로 반영 (§6.4)

---

## 9. 연간 업데이트 프로세스 (매년 8월 2일)

시공능력평가는 매년 7월 말 공시된다. **8월 2일**을 갱신 기준일로 한다.

### 9.1 자동 알림
- GitHub Actions (`.github/workflows/annual-update-reminder.yml`)가 매년 8월 2일 07:00 KST에 갱신 체크리스트가 담긴 이슈를 자동 생성한다. (수동 실행: workflow_dispatch)

### 9.2 갱신 절차 (runbook)
1. 5개 협회 사이트에서 신년도 파일 다운로드 → `RAW_new/`에 원본 그대로 보관 (파일 포맷·구조가 §3.1과 달라졌는지 확인)
2. KECA 크롤링: §4.6 체크리스트 확인 → `scripts/crawl_keca.py` 소량 테스트(3페이지) → 전체 실행 → 결과를 `raw/electrical.csv` 표준 헤더로 변환
3. `python scripts/convert_raw.py` — 원본 → 표준 raw CSV 변환 (구조 변경 시 스크립트의 해당 소스 함수 수정)
4. `python scripts/normalize.py` — 품질 검증 통과 확인 (실패 시 원인 해결 전 진행 금지)
5. `python scripts/build_html.py --year <연도>` — 신규 HTML 생성
6. `dataset-map.json` 행수·연도 갱신, 커밋·푸시
7. 조회기에서 표본 검증: 각 대분류 1위 업체가 협회 공시와 일치하는지 확인

---

## 10. 저장소 구조

```
Construction_data_new/
├── docs/PRD.md                      # 본 문서
├── RAW_new/                         # 협회 제공 원본 (xlsx·csv, §3.1)
├── raw/                             # 표준 raw CSV (6개, §5.1 헤더)
├── data/                            # 파이프라인 중간 산출물 (git 미추적)
├── scripts/
│   ├── crawl_keca.py                # KECA 크롤러 (전기설비)
│   ├── convert_raw.py               # 원본 → 표준 raw CSV 변환
│   ├── normalize.py                 # 정규화 + 품질 검증
│   ├── build_html.py                # HTML 조회기 생성
│   └── search_template.html         # 조회기 템플릿
├── .github/workflows/
│   └── annual-update-reminder.yml   # 매년 8/2 갱신 알림
├── construction_capability_search_v5.html   # 최종 산출물
├── dataset-map.json                 # 데이터셋 메타
└── not_in_use/                      # 구버전 보관
```

---

## 11. 미결사항 (Open Items)

| # | 항목 | 상태 | 담당 |
|---|---|---|---|
| O1 | `raw/electrical.csv`가 빈 파일 — 크롤링 산출물 미보관. 현재 v5는 v4 내장 데이터에서 복원한 값 사용(`normalize.py --fallback-html`). 크롤링 원본 CSV 확보 시 교체 | **부분 해결** (전기설비만 잔여, 전문건설은 원본으로 해결됨) | 사용자 |
| O2 | 사업자등록번호 결과 화면 표시 | **폐기** — 2025 원본 6종 모두에 사업자번호 없음 확인. 조건부 표시 코드는 유지(향후 자동 대응) | 완료 |
| O3 | 5개 협회 파일 다운로드 정확한 메뉴 경로/URL 기록 | 다음 수집(2026-08-02) 시 기록 | 사용자 |
| O4 | 전문건설·기계설비 순위 재산정(§6.4) 적용 승인 — v4 대비 두 대분류의 순위값이 달라짐 (원본 연번이 가나다순이었음) | 적용됨, 이견 시 `rank_policy: source`로 되돌림 | 사용자 확인 |
| O5 | `전문건설업시공능력평가공시(2025)_주력분야.xlsx` (19개 주력분야별 공시) 사용 여부 — 현재 미사용. 조회기에 추가하려면 대분류 또는 중분류 체계 결정 필요 | **미결** | 사용자 |
